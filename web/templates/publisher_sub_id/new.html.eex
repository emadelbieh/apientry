<h2>New publisher sub id</h2>

<%= render "form.html", changeset: @changeset,
                        action: publisher_sub_id_path(@conn, :create) %>

<%= link "Back", to: publisher_sub_id_path(@conn, :index) %>
